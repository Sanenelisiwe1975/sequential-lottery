{
  "name": "lottery-smart-contract",
  "version": "1.0.0",
  "description": "Tiered Sequential Lottery with Chainlink VRF",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy-lottery-vrf.js --network sepolia",
    "deploy:mumbai": "hardhat run scripts/deploy-lottery-vrf.js --network mumbai",
    "verify:sepolia": "hardhat verify --network sepolia",
    "verify:mumbai": "hardhat verify --network mumbai"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "ethers": "^6.10.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0",
    "dotenv": "^16.3.1"
  }
}
